These are my dotfiles. Most of it are taken from internet (thanks github) from awesome people.

Have fun.

To install: run "ruby install.rb"
To uninstall: run "ruby uninstall.rb"

To store credentials and project specific configurations I use "project_path" which is hooked with bashrc.